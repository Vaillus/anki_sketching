[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "anki_sketching"
version = "0.0.1"
description = "A web interface for sketching Anki card relationships."
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
